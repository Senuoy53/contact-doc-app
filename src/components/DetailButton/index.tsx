import { DetailButtonWrapper } from "./DetailButtonWrapper";

const DetailButton = ({ children }: DetailButtonProps) => {
  return <DetailButtonWrapper>{children}</DetailButtonWrapper>;
};

export default DetailButton;
